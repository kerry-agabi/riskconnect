---
description: Pick the next highest-value RiskLens MVP implementation task
argument-hint: [optional focus area]
---

Read `docs/delivery-tasks.md`, `docs/README.md`, and the current repo tree. Pick the next smallest implementation slice for `$ARGUMENTS` or the overall MVP if no focus is given.

Return:

- Current phase.
- Files likely touched.
- Acceptance criteria.
- Exact checks to run.
- Recommended subagent to use.

Keep the response under 20 bullets.

